/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'popmenucloud.com', 'images.ctfassets.net'], 
  },
}

module.exports = nextConfig
